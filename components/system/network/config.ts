// Живая система — БАЗОВЫЕ ручки (физика/радиусы/цвет). Сценовые отличия
// (формация/плотность/палитра-поверхность) живут в lib/scenes.ts; здесь — то,
// что общо для всех сцен.
//
// v7: сеть растянута за ВСЮ страницу (fixed canvas) и рекоммутируется по скроллу.
// Рендер на NormalBlending (корректно и на тёмной подложке, и на светлом стекле) —
// поэтому свечение собираем в шейдере, а не аддитивным блендингом.

export const NETWORK = {
  // — плотность (кол-во узлов от площади вьюпорта) —
  density: { divisorDesktop: 2.6, divisorMobile: 4.2, max: 116, maxMobile: 60, min: 30 },
  mobileBreakpoint: 7, // ширина вьюпорта в мировых единицах: меньше → упрощённая сеть

  // — физика (мягкая: узлы держатся у «дома», который мигрирует между сценами) —
  spring: 3.2, // жёсткость возврата к rest
  friction: 0.86, // затухание скорости за кадр
  pull: 1.9, // сила тяги к курсору
  clearZone: 0.26, // доля influence: чистая зона у курсора (узлы не слипаются)
  breath: 0.16, // базовая амплитуда «дыхания» (умножается на scene.breath)

  // — скорости рекоммутации (лерп к сценовым целям) —
  scene: {
    restRate: 2.0, // как быстро узлы мигрируют к новой формации (домам)
    visRate: 2.4, // как быстро гаснут/проявляются узлы при смене плотности
    dimRate: 2.2, // как быстро меняется интенсивность сети (scene.dim)
    paramRate: 2.4, // breath/linkDist
  },

  // — радиусы (доли min(vw, vh)) —
  influence: 0.24, // радиус реакции на курсор
  linkDist: 0.19, // базовая макс. дистанция связи (умножается на scene.linkDist)

  // — визуал —
  baseHeatChance: 0.16, // доля «горячих» узлов (тихий пульс в покое)
  heatFromPointer: 0.85, // яркость отклика у курсора
  pointerRayOpacity: 0.3, // зелёные лучи к курсору
  line: { baseAlphaDark: 0.16, hotMix: 0.7 }, // базовая прозрачность связей / подмешивание зелёного

  // — ВОЛНА СВЕТА: клик + «импульс рекоммутации» при смене сцены —
  wave: {
    speed: 1.0, // radius = t * minDim * speed
    life: 1.4, // сек жизни
    band: 0.55, // толщина кольца (доля influence)
    nodeHeat: 1.0, // вспышка узла на фронте
    linkBoost: 0.9, // доп. свечение связи на фронте
    maxLive: 5,
  },

  // — палитра. Сеть живёт на тёмной подложке → всегда светлая/зелёная (видна);
  //   контраст-закон соблюдён: зелёный = свет/вспышка на тёмном, текст на стекле —
  colors: {
    nodeOnDark: [0.855, 0.839, 0.8] as const, // костяной — свет узлов на тёмном
    lineOnDark: [0.855, 0.839, 0.8] as const, // костяные связи
    signal: [0, 0.878, 0.541] as const, // сигнальный зелёный (свет/вспышка)
    wave: [0.098, 0.949, 0.604] as const, // когерентный фронт волны
  },
} as const;
